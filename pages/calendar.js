import React from 'react'
import Layout from '../components/layout'
import { Header } from 'semantic-ui-react'

function Calendar() {
  return (
    <Layout left={<p>CALENDAR</p>} right={<p>CALENDAR</p>}>
      <Header>CALENDAR</Header>
    </Layout>
  )
}

export default Calendar
